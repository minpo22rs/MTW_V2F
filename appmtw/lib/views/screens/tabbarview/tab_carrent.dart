import 'package:flutter/material.dart';

class TabCarrent extends StatelessWidget {
  const TabCarrent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
